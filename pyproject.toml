[project]
name = "mainframe-validator"
version = "0.1.0"
description = "Monorepo for mainframe file parsing and formula validation"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "file-parser",
    "formula-engine",
]

[tool.uv.workspace]
members = ["packages/file_parser", "packages/formula_engine"]

[tool.uv.sources]
file-parser = { workspace = true }
formula-engine = { workspace = true }

[tool.pyright]
stubPath = "packages/file_parser/typings"
include = ["packages/file_parser/src", "packages/formula_engine/src", "benchmark.py"]
extraPaths = ["packages/file_parser/src", "packages/formula_engine/src"]
pythonVersion = "3.12"
pythonPlatform = "Linux"
venvPath = "."
venv = ".venv"

[tool.ruff]
exclude = ["notebooks"]

[tool.ty.environment]
extra-paths = ["packages/file_parser/typings"]
python-version = "3.12"

[tool.ty.src]
exclude = ["notebooks"]

[tool.pytest.ini_options]
testpaths = [
    "packages/file_parser/tests",
    "packages/formula_engine/tests",
]
markers = [
    "huge: multi-GB fixed file integration (requires pytest --run-huge)",
]

[dependency-groups]
dev = [
    "pre-commit>=4.0.0",
    "pytest>=9.0.3",
    "ruff>=0.15.12",
    "ty>=0.0.34",
    "ipykernel>=7.2.0", # for notebooks
]
